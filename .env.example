# Desarrollo local (docker-compose.yml)
PUERTO=127.0.0.1:8080

# Producción: este .env lo escribe deploy.yml desde los secrets de GitHub.
# Se deja aquí solo para saber qué existe; nunca con valores.
GITHUB_REPOSITORY=
IMAGE_TAG=
PROXY_NETWORK=proxy
